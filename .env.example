# Example environment variables for discussionForum
# Use either MONGO_ATLAS_URI (Atlas) or MONGO_LOCAL_URI (local Mongo) depending on your setup.
# Do NOT commit real credentials to source control.

# Local MongoDB (example)
MONGO_LOCAL_URI=mongodb://localhost:27017

# MongoDB Atlas (example) - uncomment and fill when using Atlas
# MONGO_ATLAS_URI=mongodb+srv://<user>:<password>@cluster0.example.mongodb.net

# Optional: override the database name (if not provided in the URI)
MONGO_DB_NAME=discussion_forum

# Server binding
HOST=localhost
PORT=3000
